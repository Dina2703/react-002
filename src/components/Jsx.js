const Jsx = () => {
  return (
    <div>
      <h2>JSX</h2>
      <p>
        JSX it's a syntax extention to JS. JSX allows to write HTML elements in
        JS and place them in the DOM. In React JSX describes how the UI should
        look like.{" "}
      </p>
    </div>
  );
};
